/*

function myage(y) {
   var currentAge = 2017 - y;
   return currentAge;
}


function useOld(name, year){
    var currentAge = myage(name)
    var blue = 67 - currentAge;
    console.log(currentAge, blue, name, year, y);
}

useOld(7,9);


 var n = 8;
 
    for(i=1; i<=n; i++)
    { 
        //Prints trailing spaces
        for(j=i; j<n; j++)
        {
            console.log(" ");
        }
 
        //Prints the pyramid pattern
        for(j=1; j<=(2*i-1); j++)
        {
            console.log("*");
        }
 
        ("<br>");

    }

    */


    
//     for (var i = 0; i < 9; i++) { var str = ''; var space = (7 - i);
//     if (i == 0) {
//     str += ' '.repeat(space + 1) + '*\n';
// }

// str += ' '.repeat(space + 1);
// var zero = 2 * i + 1;
// str += '0'.repeat(zero);

// console.log(str);



// fucntion power(base, exponent){
//     var result =1;
//     for (var count = 0; count < exponent; count++)
//     result *= base;
//     return result;
// }

// power (2,10);


function useOld(name, year){
    var currentAge = myage(name)
    var blue = 67 - currentAge;
    console.log(blue);
}

useOld(1,12);